name: salon_dashboard_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_database: ^11.1.0
  cloud_firestore: ^5.4.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.4
  syncfusion_flutter_charts: ^32.2.8
  action_slider: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true